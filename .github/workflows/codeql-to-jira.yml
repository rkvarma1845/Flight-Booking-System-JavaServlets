name: CodeQL Findings to Jira

on:
  workflow_run:
    workflows: ["CodeQL"]
    types:
      - completed

permissions:
  security-events: read
  contents: read

jobs:
  create-jira-ticket:
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-latest

    steps:
      - name: Get CodeQL alerts
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          echo "🔍 Fetching CodeQL alerts for repository: ${{ github.repository }}"

          curl -s \
            -H "Authorization: Bearer $GH_TOKEN" \
            -H "Accept: application/vnd.github+json" \
            https://api.github.com/repos/${{ github.repository }}/code-scanning/alerts?state=open \
            > alerts.json

          echo "📄 Alerts saved to alerts.json"
          echo "📊 Total alerts found:"
          jq 'length' alerts.json || echo "⚠️ Failed to count alerts"

      - name: Create Jira tickets
        run: |
          echo "🚀 Starting Jira ticket creation step"
      
          if ! jq -e 'type == "array"' alerts.json > /dev/null; then
            echo "ℹ️ alerts.json is not an array or empty. Exiting."
            exit 0
          fi
      
          TOTAL_ALERTS=$(jq 'length' alerts.json)
          echo "🔢 Processing $TOTAL_ALERTS alerts"
      
          jq -c '.[] | select(.rule != null) | @base64' alerts.json | while read -r encoded; do
            decode() {
              echo "$encoded" | base64 --decode | jq -r "$1"
            }
      
            id=$(decode '.number')
            title=$(decode '.rule.name // "CodeQL issue"')
            severity=$(decode '.rule.severity // "unknown"')
            file=$(decode '.most_recent_instance.location.path // "unknown file"')
            url=$(decode '.html_url')
      
            summary="CodeQL #$id – $title ($file)"
      
            echo "--------------------------------------------"
            echo "📝 Creating Jira ticket"
            echo "ID       : $id"
            echo "Title    : $title"
            echo "Severity : $severity"
            echo "File     : $file"
            echo "URL      : $url"
            echo "Summary  : $summary"
            echo "--------------------------------------------"
      
            JSON_PAYLOAD=$(jq -n \
              --arg project "${{ secrets.JIRA_PROJECT_KEY }}" \
              --arg summary "$summary" \
              --arg severity "$severity" \
              --arg file "$file" \
              --arg url "$url" \
              '{
                fields: {
                  project: { key: $project },
                  summary: $summary,
                  description: {
                    type: "doc",
                    version: 1,
                    content: [
                      {
                        type: "paragraph",
                        content: [
                          {
                            type: "text",
                            text: ("Severity: " + $severity + "\nFile: " + $file + "\n" + $url)
                          }
                        ]
                      }
                    ]
                  },
                  issuetype: { name: "Bug" }
                }
              }'
            )
            
            RESPONSE=$(curl -s -w "\nHTTP_STATUS:%{http_code}" -X POST \
              -u "${{ secrets.JIRA_EMAIL }}:${{ secrets.JIRA_API_TOKEN }}" \
              -H "Content-Type: application/json" \
              https://${{ secrets.JIRA_BASE_URL }}/rest/api/3/issue \
              -d "$JSON_PAYLOAD"
            )

      
            if echo "$RESPONSE" | grep -q "HTTP_STATUS:201"; then
              echo "✅ Jira ticket created successfully"
            else
              echo "❌ Failed to create Jira ticket"
              echo "$RESPONSE"
            fi
          done
      
          echo "🎉 Jira ticket creation process completed"
