name: CodeQL Findings to Jira

on:
  workflow_run:
    workflows: ["CodeQL"]
    types:
      - completed

permissions:
  security-events: read
  contents: read

jobs:
  create-jira-ticket:
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-latest

    steps:
      - name: Get CodeQL alerts
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          echo "🔍 Fetching CodeQL alerts for repository: ${{ github.repository }}"

          curl -s \
            -H "Authorization: Bearer $GH_TOKEN" \
            -H "Accept: application/vnd.github+json" \
            https://api.github.com/repos/${{ github.repository }}/code-scanning/alerts?state=open \
            > alerts.json

          echo "📄 Alerts saved to alerts.json"
          echo "📊 Total alerts found:"
          jq 'length' alerts.json || echo "⚠️ Failed to count alerts"

      - name: Collect Jira ticket summaries
        id: collect_jira_summaries
        run: |
          echo "📥 Fetching existing Jira issues"
      
          SEARCH_RESPONSE=$(curl -s \
            -u "${{ secrets.JIRA_EMAIL }}:${{ secrets.JIRA_API_TOKEN }}" \
            -G \
            --data-urlencode "jql=project=${{ secrets.JIRA_PROJECT_KEY }} ORDER BY created DESC" \
            --data-urlencode "fields=summary" \
            --data-urlencode "maxResults=1000" \
            "https://${{ secrets.JIRA_BASE_URL }}/rest/api/3/search")
      
          echo "📊 Total issues found:"
          echo "$SEARCH_RESPONSE" | jq '.total'
      
          # Extract summaries into a list
          echo "$SEARCH_RESPONSE" \
            | jq -r '.issues[].fields.summary' \
            > jira_summaries.txt
      
          echo "📝 Collected summaries:"
          cat jira_summaries.txt
      
          # Export as multiline output (usable in next steps)
          {
            echo "summaries<<EOF"
            cat jira_summaries.txt
            echo "EOF"
          } >> "$GITHUB_OUTPUT"
